REPORT ZPRG30_EX06 MESSAGE-ID ZA.

PARAMETERS: P_PAIS LIKE T005S-LAND1.

TABLES T005H.

SELECT * FROM T005H WHERE LAND1 = P_PAIS ORDER BY CITYC.
  WRITE: / T005H-BEZEI,
         / T005H-LAND1.
ENDSELECT.

IF SY-SUBRC NE 0.
  MESSAGE I000.
ENDIF.
